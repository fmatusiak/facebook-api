package com.fmatusiak.facebookapi.service.post;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

@SpringBootTest
@RunWith(SpringRunner.class)
public class PostFinderControllerTest {
    /*@Autowired
    private PostFinderService postFinderService;*/

    @Test
    public void getPublicPostsList()/* throws FacebookException */ {
        /*//given & when
        Post post = postFinderService.getAllPublicPosts().get(0);
        List<Post> postList = postFinderService.getPublicPostsListByName(post.getName());
        //then
        for(Post readPost : postList){
            Assert.assertNotNull(readPost);
        }*/
    }

}